# --- core automation (fast, native) ---
pyautogui==0.9.54
# PyScreeze (pyautogui's screenshot backend) imports Pillow lazily and doesn't
# always pull it in transitively -- pin it explicitly or `screenshot` silently
# fails with "PyAutoGUIException: unable to import pyscreeze".
Pillow==11.0.0
pydirectinput==1.0.4
pygetwindow==0.0.9
pyperclip==1.9.0
psutil==6.1.0
keyboard==0.13.5
mouse==0.7.1

# --- speech (offline, low latency) ---
vosk==0.3.45
sounddevice==0.5.1
numpy==2.1.3
pyttsx3==2.98

# --- matching / parsing ---
rapidfuzz==3.10.1

# --- windows integration ---
pywin32==308 ; sys_platform == "win32"
comtypes==1.4.8 ; sys_platform == "win32"
pycaw==20240210 ; sys_platform == "win32"
# Windows' own offline OCR, for finding things on screen that an app does
# not publish to the accessibility tree (Chrome's profile picker, web pages).
winsdk==1.0.0b10 ; sys_platform == "win32"

# --- optional cloud brain (fallback planner) ---
httpx==0.28.1
